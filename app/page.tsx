"use client";

import Link from "next/link";
import { ArrowRight, Check, CheckCircle2, Compass, Menu, Search, ShieldCheck, Sparkles, Users, X } from "lucide-react";
import { useEffect, useState } from "react";
import { useSession } from "next-auth/react";
import AssetCard from "@/components/asset-card";
import { assets } from "@/lib/data";
import { formatPostDate, initials, postUrl, profileUrl } from "@/lib/social";

type Feed = { posts: { id: string; authorUsername: string; authorName: string | null; authorImage: string | null; verified: boolean; text: string; images: string[]; createdAt: string }[]; people: { username: string; name: string | null; image: string | null; verified: boolean; followers: number }[] };

function AuthenticatedHome() {
  const { data: session } = useSession(); const [feed, setFeed] = useState<Feed>({ posts: [], people: [] }); const [profile, setProfile] = useState<{ name: string; username: string } | null>(null); const [loading, setLoading] = useState(true);
  useEffect(() => { Promise.all([fetch("/api/feed", { cache: "no-store" }).then(r => r.json()), fetch("/api/profile", { cache: "no-store" }).then(r => r.json())]).then(([network, me]) => { setFeed(network?.posts ? network : { posts: [], people: [] }); if (me?.username) setProfile({ name: me.name || "Member", username: me.username }); }).catch(() => undefined).finally(() => setLoading(false)); }, []);
  const name = profile?.name || session?.user?.name || "Member"; const ownProfileUrl = profileUrl(profile?.username || name);
  return <main className="socialHome"><section className="socialHomeHero"><div><span className="kicker">YOUR TRUST.ME</span><h1>Good to see you, <em>{name.split(" ")[0]}.</em></h1><p>Your network, verified opportunities and the people shaping Trust.Me — in one place.</p></div><Link className="primary" href={ownProfileUrl}>Open your profile <ArrowRight size={16}/></Link></section><div className="socialHomeGrid"><section className="socialFeedPanel"><div className="feedHeading"><div><span className="kicker">YOUR FEED</span><h2>Latest from your network</h2></div><Link href="/people"><Users size={15}/> Find people</Link></div>{loading ? <div className="socialEmpty"><p>Loading your network…</p></div> : feed.posts.length === 0 ? <div className="socialEmpty"><Compass size={25}/><h3>Your network starts here.</h3><p>Follow people you trust and their posts will appear here.</p><Link className="secondary" href="/people">Discover people</Link></div> : feed.posts.slice(0, 8).map(post => <article className="homePost" key={post.id}><Link href={profileUrl(post.authorUsername)} className="homePostAvatar">{post.authorImage ? <img src={post.authorImage} alt=""/> : initials(post.authorName || post.authorUsername)}</Link><div className="homePostBody"><div><Link href={profileUrl(post.authorUsername)}><strong>{post.authorName || post.authorUsername}</strong></Link>{post.verified && <span className="verifiedDot"><Check size={9}/></span>}<span> · <Link href={postUrl(post.authorUsername, post.id)}>{formatPostDate(post.createdAt)}</Link></span></div>{post.text && <p>{post.text}</p>}{post.images.length > 0 && <div className="homePostImages">{post.images.slice(0, 4).map((src, i) => <img src={src} alt="Post media" key={i}/>)}</div>}<Link className="homePostLink" href={postUrl(post.authorUsername, post.id)}>Open post <ArrowRight size={13}/></Link></div></article>)}</section><aside className="socialHomeAside"><section className="discoverPanel"><span className="kicker">NETWORK</span><h2>Most followed</h2><p className="panelSub">The members with the largest audiences.</p>{feed.people.map((p, i) => <Link className="homePerson" href={profileUrl(p.username)} key={p.username}><span className="homeRank">{String(i + 1).padStart(2, "0")}</span><span className="personAvatar">{p.image ? <img src={p.image} alt=""/> : initials(p.name || p.username)}</span><span className="personMeta"><strong>{p.name || p.username} {p.verified && <span className="verifiedDot"><Check size={9}/></span>}</strong><span>@{p.username}</span></span><span className="followCount"><strong>{p.followers}</strong> followers</span></Link>)}</section><section className="discoverPanel"><span className="kicker">MARKETPLACE</span><h2>Verified assets</h2><p className="panelSub">Explore what is available now.</p>{assets.slice(0, 2).map(a => <Link className="homeAsset" href={`/assets/${a.id}`} key={a.id}><span>{a.type}</span><strong>{a.title}</strong><small>{a.price}</small></Link>)}</section></aside></div></main>;
}

export default function Home() {
  const { status } = useSession(); const [menuOpen, setMenuOpen] = useState(false);
  if (status === "authenticated") return <AuthenticatedHome/>;
  return <main><nav className="nav"><Link className="logo" href="/">Trust<span>.</span>Me</Link><div className={menuOpen ? "navLinks mobileOpen" : "navLinks"}><Link href="/assets" onClick={() => setMenuOpen(false)}>Explore Assets</Link><Link href="/how-it-works" onClick={() => setMenuOpen(false)}>How It Works</Link><Link href="/sell" onClick={() => setMenuOpen(false)}>Sell an Asset</Link><Link href="/about" onClick={() => setMenuOpen(false)}>About</Link></div><div className="navActions"><Link className="searchBtn" href="/assets" aria-label="Search"><Search size={18}/></Link><Link className="login" href="/login">Log in</Link><Link className="primary small" href="/signup">Get Started</Link><button className="menuBtn" onClick={() => setMenuOpen(!menuOpen)} aria-label="Menu">{menuOpen ? <X size={21}/> : <Menu size={21}/>}</button></div></nav><section className="hero" id="top"><div className="heroGlow"/><div className="heroCopy"><div className="eyebrow"><ShieldCheck size={15}/> Egypt&apos;s marketplace for verified assets</div><h1>Valuable assets deserve a higher <em>standard.</em></h1><p>Discover, evaluate, and acquire valuable assets with verified ownership, documentation, and provenance — in one trusted marketplace.</p><div className="heroCtas"><Link className="primary" href="/assets">Explore verified assets <ArrowRight size={17}/></Link><Link className="secondary" href="/sell">List an asset</Link></div><div className="trustLine"><CheckCircle2 size={16}/> Trust.Me is verification-first by design.</div></div><div className="heroVisual" aria-hidden="true"><div className="passportCard"><div className="passportTop"><span>TRUST.ME</span><span>ASSET PASSPORT</span></div><div className="passportSeal"><ShieldCheck size={38}/></div><div className="passportTitle">VERIFIED ASSET</div><div className="passportLine"/><div className="passportLine short"/><div className="passportBottom"><span>OWNERSHIP</span><strong>VERIFIED</strong></div></div></div></section><section className="stats"><div><strong>100%</strong><span>Verification-first</span></div><div><strong>EGP</strong><span>Built for Egypt</span></div><div><strong>24/7</strong><span>Access to opportunities</span></div><div><strong>1</strong><span>Trusted destination</span></div></section><section className="section" id="assets"><div className="sectionHead"><div><span className="kicker">CURATED MARKETPLACE</span><h2>Assets worth <em>trusting.</em></h2></div><Link className="textLink" href="/assets">View all assets <ArrowRight size={16}/></Link></div><div className="assetGrid">{assets.slice(0, 3).map(asset => <AssetCard key={asset.id} asset={asset}/>)}</div></section><section className="passportSection" id="how"><div className="passportText"><span className="kicker">THE TRUST.ME STANDARD</span><h2>Every asset has a <em>story.</em><br/>We verify the evidence.</h2><p>The Asset Passport brings ownership, documentation, provenance, verification status, and essential details together into one clear record.</p><div className="checks"><span><CheckCircle2/> Identity reviewed</span><span><CheckCircle2/> Ownership reviewed</span><span><CheckCircle2/> Documentation checked</span><span><CheckCircle2/> Provenance recorded</span></div><Link className="secondary light" href="/how-it-works">Explore the verification standard <ArrowRight size={17}/></Link></div><div className="miniPassport"><div className="miniHeader">TRUST.ME <span>01 / 04</span></div><div className="miniCircle"><ShieldCheck size={30}/></div><p>ASSET PASSPORT</p><h3>Verification<br/>Complete</h3><div className="miniStamp">✓ VERIFIED</div></div></section><section className="creatorTeaser"><div><span className="kicker">VERIFIED DISTRIBUTION</span><h2>Discovery needs <em>context.</em></h2><p>Verified creators can discover, explain, and distribute approved opportunities to audiences that trust them.</p></div><Link className="secondary" href="/creators">Explore the creator network <ArrowRight size={17}/></Link></section><section className="cta" id="sell"><Sparkles size={18}/><h2>Have something valuable?</h2><p>Bring your asset to a marketplace built around verification, transparency, and serious buyers.</p><Link className="primary" href="/sell">List an asset <ArrowRight size={17}/></Link></section><footer id="about"><div><Link className="logo" href="/">Trust<span>.</span>Me</Link><p>Verified. Valuable. Yours.</p></div><div className="footerLinks"><Link href="/assets">Explore</Link><Link href="/how-it-works">Verification</Link><Link href="/creators">Creators</Link><Link href="/sell">Sell</Link><Link href="/about">About</Link></div><div className="copyright">© 2026 Trust.Me</div></footer></main>;
}
